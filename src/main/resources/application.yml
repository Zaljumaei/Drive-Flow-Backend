spring:
  datasource:
    url: jdbc:h2:file:./data/mydb;AUTO_SERVER=TRUE
    username: sa
    password: password
    driverClassName: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true
    properties:
      hibernate.hibernate.format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console




paging:
  page:
    drivingSchoolPageSize: 50
    instructorPageSize: 20
    studentPageSize: 20
    studentEnrollmentPageSize: 20
    vehiclePageSize: 20
